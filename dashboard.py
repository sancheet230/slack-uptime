def format_hours_minutes(seconds):\n    hours = seconds // 3600\n    minutes = (seconds % 3600) // 60\n    return f'{hours} hours and {minutes} minutes'\n\n# Example of usage\nprint(format_hours_minutes(7265))  # Outputs: '2 hours and 1 minutes'\n